version: '3'
services:
  reverseproxy:
    build:
      context: .
    image: lemix777/reverseproxy
  backend_user:
    build:
      context: ../../udacity-c3-restapi-user
    image: lemix777/udacity-restapi-user
  backend_feed:
    build:
      context: ../../udacity-c3-restapi-feed
    image: lemix777/udacity-restapi-feed
  frontend:
    build:
      context: ../../udacity-c3-frontend
    image: lemix777/udacity-frontend:local
